Beste <%= @user.name %>

Er is een automatische saldomail verstuurd naar alle gebruikers die een negatief saldo hebben. Er zijn <%= @send_to_count %> gebruikers gemaild.

<% if @mail_unknown_user.any? %>
  De volgende gebruikers hebben een negatief saldo maar geen mailadres in het systeem. Deze gebruikers zijn niet geinformeerd over hen negatieve saldo.
  <% @mail_unknown_user.each do |user| %>
      - <%= user.name %>
  <% end %>

<% end %>
Met vriendelijke groet,
Webmeester C.S.V. Alpha
