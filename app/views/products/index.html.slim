.container

  h1 = 'Products'

  table.table
    thead
      tr
        td = 'Name'
        td = 'Created at'
        td = 'Updated at'
    tbody
      - @model.each do |product|
        tr
          td
            =product.name
          td
            =datetime(product.created_at)
          td
            =datetime(product.created_at)
