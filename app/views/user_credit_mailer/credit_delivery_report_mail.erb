Beste <%= @user.name %>

Op <%= l Time.zone.now, format: :long %> is er een automatische saldomail verstuurd naar alle gebruikers die een negatief saldo hebben. Er zijn <%= @success_count %> gebruikers gemaild.

<% if @unnotifyable_users.any? %>
  De volgende gebruikers hebben een negatief saldo maar geen mailadres in het systeem. Deze gebruikers zijn niet geinformeerd over hen negatieve saldo.
  <% @unnotifyable_users.each do |user_name| %>
      - <%= user_name %>
  <% end %>

<% end %>
Met vriendelijke groet,
Webmeester C.S.V. Alpha
